# Repository Instructions

- Use the project virtual environment in `venv/` for Python commands.
- Prefer `venv/bin/python -m pytest` for tests instead of the system `pytest`.
- Prefer `venv/bin/python -m py_compile ...` for compile checks.
